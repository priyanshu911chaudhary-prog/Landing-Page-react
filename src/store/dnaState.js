export const dnaState = {
  // Base transform
  x: 0,
  y: 0,
  z: 0,
  rotationX: 0.4,
  rotationY: 0,
  rotationZ: -0.5,
  scale: 0.96,
  
  // Camera
  cameraZ: 8.4,
  
  // Helix properties (can be animated if we recreate geometry, but usually static is fine)
  twists: 5,
};
